version: "3"
services:
  phpfpm:
    build: ./phpfpm
    volumes:
      - ./phpfpm/src:/var/www/html
    environment:
      url: "${GET_URL}"
      auth: "${AUTH}"

  web:
    build: ./web
    ports:
      - "${PORT}:80"
    depends_on:
      - phpfpm